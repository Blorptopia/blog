[project]
name = "site"
version = "0.1.0"
description = ""
authors = []
requires-python = ">=3.14"
dependencies = [
    "jinja2 (>=3.1.6,<4.0.0)"
]

[tool.poetry]
package-mode = false


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
